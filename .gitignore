# secrets — never commit
.env
.env.*
!.env.example
*.keypair.json

# deps / build
node_modules/
dist/
.next/
.next-dev/
*.tsbuildinfo

# local data
pgdata/
*.log
.gstack/

# QUARANTINE — prompt-injection research artifact, never commit or extract into the project
agent-os-pack-*.zip
agents/agent-os-pack/
.hermes-*.pid
